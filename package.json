{
  "name": "tiktok-be",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "build": "bun build --minify-whitespace --minify-syntax --target bun --outfile ./dist/index.js ./src/index.ts",
    "start": "bun run build && bun run --watch dist/index.js"
  },
  "dependencies": {
    "axios": "^1.9.0",
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "module": "src/index.js"
}